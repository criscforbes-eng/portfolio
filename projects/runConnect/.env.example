#environment variables my app expects (names and example values)

# Key for mongoDb
MONGODB_URI= mongodb+srv://<user>:<db_password>@poo2025c3.y5umzxd.mongodb.net/
# Replace <user> with the mongoDB user name 
# Replace <db_password> with the password for the user

# local Port
PORT=3000


JWT_SECRET=replace_with_a_secure_random_string
NODE_ENV=development
# Add other keys your app requires, e.g.
# SESSION_SECRET=...
# API_KEY=...